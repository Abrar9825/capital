{
  "name": "capital-billing-system-backend",
  "version": "1.0.0",
  "description": "Backend API for Capital Billing System - Clothing Store POS",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node tests/apiTests.js",
    "cleanup": "node -e \"const {cleanDatabase} = require('./utils/dbCleanup'); require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.MONGODB_URI).then(() => cleanDatabase().then(() => process.exit(0)));\""
  },
  "keywords": [
    "billing",
    "pos",
    "clothing",
    "inventory",
    "sales"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@octokit/rest": "^20.1.2",
    "axios": "^1.13.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.10",
    "express": "^4.18.2",
    "mongoose": "^9.0.1",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
